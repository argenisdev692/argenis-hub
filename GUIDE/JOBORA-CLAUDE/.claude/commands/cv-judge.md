Quick recruiter/ATS audit only — no job search, no rewrite file yet, no
GitHub. Use `.claude/skills/cv-job-studio/references/scoring-rubric.md` §1–2.

1. Pick the CV to audit: use the file I named below this line, or if none,
   ask whether to use the base CV in `fullstack cv/` or a file in
   `other cv/`.
2. Use sequential-thinking to reason through the resume before answering.
3. Give me a blunt Fortune-500-recruiter 10-second-scan verdict: PASS /
   BORDERLINE / REJECT RISK, with the specific reasons — what stands out,
   what's forgettable, what would make a recruiter move on to the next
   candidate before finishing the page.
4. List: `target_job_title`, `strengths`, `improvements`, `keyword_gaps`,
   `xyz_gaps` (bullets missing a measurable result), and up to 6
   `metric_questions` I could answer to unlock stronger bullets — only if an
   honest estimate is plausible, never invent numbers.
5. Do not rewrite the CV in this command — that's `/cv-job-search` or
   `/cv-job-search-other`. This is audit-only, for fast iteration.

CV file / target role, if specified, follow below this line:
